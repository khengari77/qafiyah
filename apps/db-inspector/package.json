{
  "name": "@qafiyah/db-inspector",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "dev": "bun run server.ts",
    "export": "bun run export.ts",
    "lint": "biome check .",
    "lint:fix": "biome check --write .",
    "format": "biome format --write .",
    "check-types": "tsc --noEmit --pretty"
  },
  "dependencies": {
    "js-yaml": "^4.1.1",
    "postgres": "^3.4.4"
  },
  "devDependencies": {
    "@types/bun": "latest"
  }
}

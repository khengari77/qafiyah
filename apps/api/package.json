{
  "name": "@qafiyah/api",
  "scripts": {
    "dev": "wrangler dev",
    "dev:test": "wrangler dev --env test",
    "build": "pnpm run build:deps && wrangler build",
    "build:deps": "cd ../../packages/schemas && pnpm run build",
    "deploy": "wrangler deploy --minify",
    "lint": "biome check .",
    "lint:fix": "biome check --write .",
    "format": "biome format --write .",
    "types": "tsc --noEmit --pretty",
    "test": "vitest run"
  },
  "dependencies": {
    "@hono/zod-validator": "^0.4.3",
    "@neondatabase/serverless": "^1.0.0",
    "@qafiyah/tsconfig": "workspace:*",
    "@qafiyah/schemas": "workspace:*",
    "drizzle-orm": "^0.41.0",
    "hono": "^4.11.4",
    "postgres": "^3.4.5",
    "zod": "^3.24.2"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20250214.0",
    "@types/node": "^25.0.10",
    "@vitest/coverage-v8": "^2.1.8",
    "@vitest/ui": "^2.1.8",
    "vitest": "^2.1.8",
    "wrangler": "^4.59.1"
  },
  "type": "module",
  "types": "./src/app.ts"
}

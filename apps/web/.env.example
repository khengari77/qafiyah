# Astro reads this file (not Wrangler's `.dev.vars`). Copy to `.env` and fill in.
# `pnpm db:setup` generates `.env` pointing at the locally-running API.

# In dev, point search/random at the local API on :8787 so it queries the
# local Docker Postgres without CORS.
PUBLIC_API_URL=http://localhost:8787
